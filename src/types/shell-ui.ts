/**
 * Second (right-hand) panel view identifiers.
 *
 * 次面板（右侧面板）可切换的视图标识：信息、文件、Git、命令大纲、传输队列。
 */
export type SecondPanelView = "info" | "files" | "git" | "outline" | "transfers";

/** Primary (left) sidebar tab: sessions or remote hosts. */
export type PrimaryPanelTab = "sessions" | "hosts";
